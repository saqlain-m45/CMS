
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Allow access to backend folder
  RewriteRule ^backend/ - [L]

  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
